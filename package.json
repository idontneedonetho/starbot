{
  "name": "starbot",
  "version": "0.1.0",
  "description": "StarPilot Discord bot — identity onboarding and report submission",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_NO_WARNINGS=1 tsx watch src/index.ts",
    "start": "NODE_NO_WARNINGS=1 tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "*",
    "discord.js": "*",
    "discordx": "*",
    "dotenv": "*",
    "minisearch": "*",
    "simple-git": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "tsx": "*",
    "typescript": "*"
  }
}
