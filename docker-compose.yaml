services:
  starbot:
    build:
      context: .
      args:
        COMMIT_HASH: ${COMMIT_HASH:-unknown}
    container_name: starbot
    init: true
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data:rw
    environment:
      - NODE_ENV=production