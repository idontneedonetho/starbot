services:
  starbot:
    build: .
    container_name: starbot
    init: true
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ../files/data:/app/data:rw
      - ./.git:/app/.git:ro
    environment:
      - NODE_ENV=production